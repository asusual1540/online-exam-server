const { Question } = require("../../models/question")
const { Answer } = require("../../models/answer")
const isAuth = require("../../middleware/is-auth")

const { dataModifier } = require("../resolvers/merger")

module.exports = {}
